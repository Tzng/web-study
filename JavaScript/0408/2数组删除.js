/**
 * 传入一个数组，删除里面的偶数，然后打印出来
 * > 示例1：[1,2,3,4,5,6] => [1,3,5]
 */
function deleteEvenNumber(x) {
    // 这里写代码
    for(var i=0;i<x.length;i++){
        if(x[i]%2){
            y[j]=x[i]
            j++;
        }
    }
    return y;
}
const y=[];
var j=0;
const arr = [1, 2, 3, 4, 5, 6];
console.log(deleteEvenNumber(arr))
